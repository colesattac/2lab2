<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kz.talipovsn.calendar">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name="kz.talipovsn.calendar.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="kz.talipovsn.calendar.SettingsActivity"
            android:label="@string/Настройки_приложения"
            android:parentActivityName="kz.talipovsn.calendar.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="kz.talipovsn.calendar.MainActivity" />
        </activity>

    </application>

</manifest>